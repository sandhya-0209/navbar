import { ToggleListDirective } from './toggle-list.directive';

describe('ToggleListDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleListDirective();
    expect(directive).toBeTruthy();
  });
});
